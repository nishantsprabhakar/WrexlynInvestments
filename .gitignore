node_modules/
dist/
*.log
.env

# Runtime data — deal records and generated documents are per-install state,
# not source.
data/
workspace/

# Inno Setup's compiled installer output — the .iss script and icons are source, this isn't.
installer/windows/output/
